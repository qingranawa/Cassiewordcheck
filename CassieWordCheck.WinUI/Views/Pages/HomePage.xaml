<local:AppPage x:Class="CassieWordCheck.Views.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:CassieWordCheck.Views.Pages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <Grid MaxWidth="1120" RowSpacing="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Spacing="6">
                <TextBlock x:Name="TitleTextBlock" Style="{StaticResource PageTitleTextBlockStyle}" />
                <TextBlock x:Name="SubtitleTextBlock" Style="{StaticResource PageSubtitleTextBlockStyle}" />
            </StackPanel>

            <Grid Grid.Row="1" ColumnSpacing="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.05*" />
                    <ColumnDefinition Width="0.95*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="14">
                        <TextBlock x:Name="InputLabelTextBlock" FontSize="20" FontWeight="SemiBold" />
                        <TextBox x:Name="InputBox"
                             PlaceholderText=""
                             AcceptsReturn="True"
                             MinHeight="260"
                             TextWrapping="Wrap"
                             TextChanged="OnInputTextChanged" />
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button x:Name="CheckButton" Click="OnCheck" Style="{StaticResource AccentButtonStyle}" />
                            <Button x:Name="ClearButton" Click="OnClear" Style="{StaticResource SecondaryButtonStyle}" />
                            <Button x:Name="ImportButton" Click="OnImport" Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                        <TextBlock x:Name="WordListStatusTextBlock" Style="{StaticResource PageSubtitleTextBlockStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <Grid RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <TextBlock x:Name="ResultLabelTextBlock" FontSize="20" FontWeight="SemiBold" />
                            <ComboBox x:Name="ResultModeCombo" Width="150" HorizontalAlignment="Right" SelectionChanged="OnModeChanged" />
                        </Grid>
                        <ProgressBar x:Name="CoverageProgressBar" Grid.Row="1" Minimum="0" Maximum="100" Height="6" />
                        <Grid Grid.Row="2" ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="CoverageTextBlock" Grid.Column="0" />
                            <TextBlock x:Name="AvailableTextBlock" Grid.Column="1" />
                            <TextBlock x:Name="UnavailableTextBlock" Grid.Column="2" />
                            <TextBlock x:Name="IgnoredTextBlock" Grid.Column="3" />
                        </Grid>
                        <ListView x:Name="ResultList"
                                  Grid.Row="3"
                                  SelectionMode="None"
                                  IsItemClickEnabled="False"
                                  BorderThickness="0" />
                        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="10">
                            <Button x:Name="CopyResultButton" Click="OnCopyResult" Style="{StaticResource SecondaryButtonStyle}" />
                            <Button x:Name="ExportResultButton" Click="OnExportResult" Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock x:Name="SuggestionTitleTextBlock" FontWeight="SemiBold" />
                    <StackPanel x:Name="SuggestionsPanel" Margin="0,10,0,0" Spacing="6" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</local:AppPage>
