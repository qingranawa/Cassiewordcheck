<Window x:Class="CassieWordCheck.Views.DiffWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       
        Title="词库对比"
        Width="700" Height="480"
        MinWidth="520" MinHeight="360"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        Loaded="OnWindowLoaded">

    <Border Margin="1" CornerRadius="8"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Grid Margin="16,12,16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Column="0">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="19" Width="18" Height="18" VerticalAlignment="Center" TextAlignment="Center" />
                            <TextBlock x:Name="TitleLabel"
                                       Text="词库对比"
                                       Style="{StaticResource SectionLabel}"
                                       FontSize="14" Margin="6,0,0,0" />
                        </StackPanel>
                <Button Grid.Column="2"
                        x:Name="LoadRightButton"
                        Style="{StaticResource ModernButton}"
                        Padding="14,6"
                        FontSize="12"
                        Click="OnLoadRightWordlist">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="17" Width="16" Height="16" VerticalAlignment="Center" TextAlignment="Center" />
                                <TextBlock Text="加载对比词库" Margin="6,0,0,0" />
                            </StackPanel>
                        </Button>
            </Grid>

            <!-- 统计概览 -->
            <Border Grid.Row="1"
                    x:Name="SummaryBar"
                    CornerRadius="8"
                    Background="{StaticResource BgBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Padding="12,8"
                    Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" CornerRadius="6" Background="#0A2E1A" Padding="12,6">
                        <TextBlock x:Name="LeftCountLabel"
                                   Text="左: 0"
                                   Foreground="{StaticResource SuccessBrush}"
                                   FontSize="11" FontFamily="{StaticResource MonoFont}" />
                    </Border>
                    <Border Grid.Column="1" CornerRadius="6" Background="#2D1B1B" Padding="12,6" Margin="8,0,0,0">
                        <TextBlock x:Name="RightOnlyCountLabel"
                                   Text="新增: 0"
                                   Foreground="{StaticResource ErrorBrush}"
                                   FontSize="11" FontFamily="{StaticResource MonoFont}" />
                    </Border>
                    <Border Grid.Column="3" CornerRadius="6" Background="#1A1A2E" Padding="12,6">
                        <TextBlock x:Name="CommonCountLabel"
                                   Text="共有: 0"
                                   Foreground="{StaticResource AccentLightBrush}"
                                   FontSize="11" FontFamily="{StaticResource MonoFont}" />
                    </Border>
                </Grid>
            </Border>

            <!-- 差异列表（双栏） -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- 左栏：仅在左词库 -->
                <Border Grid.Column="0"
                        CornerRadius="8"
                        Background="{StaticResource BgBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="12,6">
                            <TextBlock x:Name="LeftSectionLabel"
                                       Text="仅左侧存在"
                                       Foreground="{StaticResource SuccessBrush}"
                                       FontSize="11" FontWeight="SemiBold" />
                        </Border>
                        <ListBox x:Name="LeftOnlyList"
                                 Grid.Row="1"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Padding="4">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               FontSize="12"
                                               FontFamily="{StaticResource MonoFont}"
                                               Foreground="{StaticResource SuccessBrush}"
                                               Padding="8,3" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- 右栏：仅在右词库 -->
                <Border Grid.Column="2"
                        CornerRadius="8"
                        Background="{StaticResource BgBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="12,6">
                            <TextBlock x:Name="RightSectionLabel"
                                       Text="仅右侧存在"
                                       Foreground="{StaticResource ErrorBrush}"
                                       FontSize="11" FontWeight="SemiBold" />
                        </Border>
                        <ListBox x:Name="RightOnlyList"
                                 Grid.Row="1"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Padding="4">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               FontSize="12"
                                               FontFamily="{StaticResource MonoFont}"
                                               Foreground="{StaticResource ErrorBrush}"
                                               Padding="8,3" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>

            <!-- 底部：状态 + 导出 -->
            <Border Grid.Row="3"
                    Margin="0,8,0,0"
                    CornerRadius="8"
                    Background="{StaticResource BgBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Padding="12,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               x:Name="StatusLabel"
                               Text="请点击「加载对比词库」选择要对比的词库文件"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="11"
                               VerticalAlignment="Center" />
                    <Button Grid.Column="1"
                            x:Name="ExportButton"
                            Style="{StaticResource ModernButton}"
                            Padding="14,6"
                            FontSize="11"
                            IsEnabled="False"
                            Click="OnExportDiff">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="15" Width="14" Height="14" VerticalAlignment="Center" TextAlignment="Center" />
                                <TextBlock Text="导出差异报告" Margin="4,0,0,0" />
                            </StackPanel>
                        </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
